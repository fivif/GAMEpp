name: Build
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: Download sing-box for bundling
        run: |
          mkdir -p src-tauri\resources
          curl -sL "https://github.com/SagerNet/sing-box/releases/download/v1.13.14/sing-box-1.13.14-windows-amd64.zip" -o sing-box.zip
          7z x sing-box.zip -osing-box-tmp -y
          Get-ChildItem -Path sing-box-tmp -Recurse -Filter sing-box.exe | ForEach-Object { Copy-Item $_.FullName src-tauri\sing-box.exe -Force }
          Write-Host "sing-box.exe copied to src-tauri\"
          Get-ChildItem src-tauri\sing-box.exe
        shell: pwsh
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'GAME++ ${{ github.ref_name }}'
          prerelease: false
          args: --target x86_64-pc-windows-msvc

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: Download sing-box for bundling
        run: |
          curl -sL "https://github.com/SagerNet/sing-box/releases/download/v1.13.14/sing-box-1.13.14-darwin-arm64.tar.gz" -o sing-box.tar.gz
          mkdir -p sing-box-tmp && tar xzf sing-box.tar.gz -C sing-box-tmp
          find sing-box-tmp -name sing-box -type f -exec cp {} src-tauri/sing-box \;
          chmod +x src-tauri/sing-box
          ls -la src-tauri/sing-box
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'GAME++ ${{ github.ref_name }}'
          prerelease: false
